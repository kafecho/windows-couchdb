[httpd]
port = {{ couchdb_port }}
bind_address = {{ couchdb_bind_address }}

# The setting below disables the Nagle's algorithm to improve CouchDB's performance. 
socket_options = [{recbuf, 262144}, {sndbuf, 262144}, {nodelay, true}]
